#Dada a lista `letras = ['a', 'b', 'c', 'd', 'e']`, converta cada letra para maiúscula.

letras = ['a', 'b', 'c', 'd', 'e']
lst = [x.upper() for x in letras]
print(lst)

#upper > converte minúscula para maiúscula
